[package]
name = "pumlr"
version = "0.1.0"
edition = "2021"
description = "A pure Rust PlantUML renderer — generates SVG from PlantUML text without Java or Graphviz"
license = "MIT"

[dependencies]
nom = "8"
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
